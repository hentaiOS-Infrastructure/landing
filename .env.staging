# Payload CMS Configuration for Staging
PAYLOAD_SECRET="your_strong_unique_staging_payload_secret_here"
DATABASE_URI="mongodb://127.0.0.1:27017/hentai-landing-payload" # Replace 'your-staging-db-name'

# Next.js Frontend Configuration for Staging
# Assuming Payload backend also runs locally for staging, adjust if different
NEXT_PUBLIC_PAYLOAD_URL="http://localhost:3000"
